name: akyron_server
description: "Servidor backend do Akyron RPG — HTTP + WebSocket. Roda standalone, fora do app."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_web_socket: ^2.0.0
  web_socket_channel: ^3.0.0
  args: ^2.5.0
  crypto: ^3.0.3
  uuid: ^4.4.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
